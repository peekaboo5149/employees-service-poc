services:
  db:
    image: postgres:15.1-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: pass
      POSTGRES_USER: user
      POSTGRES_DB: employee
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
    driver: local